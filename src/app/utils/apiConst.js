// @flow

const ARTICLES_SEARCH = "svc/search/v2/articlesearch.json";

/**
 * Enum with all api endpoints
 * @readonly
 * @enum {string}
 */
export const Endpoints = Object.freeze({
  ARTICLES_SEARCH
});
